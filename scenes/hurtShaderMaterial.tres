[gd_resource type="ShaderMaterial" load_steps=2 format=3 uid="uid://dc4hfw2g0aq36"]

[sub_resource type="Shader" id="Shader_f64hl"]
code = "shader_type canvas_item;


uniform float amount : hint_range(0, 1);
uniform vec4 hurtColor : source_color = vec4(1.0);

float lerp(float a, float b,float t){
	return  a + (b - a) * t;
}

void fragment() {
	vec4 previous_color = texture(TEXTURE, UV);
	
	vec4 adjustedColor = vec4(lerp(previous_color.r,hurtColor.r,amount), lerp(previous_color.g,hurtColor.g,amount), lerp(previous_color.b,hurtColor.b,amount), previous_color.a);
	
	
	COLOR = adjustedColor;
}


"

[resource]
resource_local_to_scene = true
shader = SubResource("Shader_f64hl")
shader_parameter/amount = 0.0
shader_parameter/hurtColor = Color(1, 0.0745098, 0.0431373, 1)
